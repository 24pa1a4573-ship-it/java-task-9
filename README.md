# java-task-9
Simple Java console app using Collections. Stores students in ArrayList, removes duplicates with Set, fast lookup with HashMap, sorts using Comparator, iterates with enhanced for loop, and prints formatted student reports.
